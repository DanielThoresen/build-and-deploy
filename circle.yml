dependencies:
  override:
    - npm install
    - bower install
  post: 
    - grunt build

test:
  override:
    - grunt test

deplayment:
prod:
  branch: master
  heroku:
    appname: {daniel-test-build-and-deploy}